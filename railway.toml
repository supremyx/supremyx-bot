[build]
builder = "RAILPACK"
buildCommand = "pnpm install --no-frozen-lockfile && cd dashboard && npx vite build --config vite.config.ts"

[deploy]
startCommand = "node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 3000
